import colors from './colors';

type Theme = {
  colors: {
    background: string;
    black: string;
    lightGray: string;
    darkGray: string;
    clay: string;
    green: string;
    red: string;
    violet: string;
    purple: string;
  };
};

const theme = {
  colors
};

export default theme;
